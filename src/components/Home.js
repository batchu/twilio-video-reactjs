import React from 'react'

function Home(){

    return <div>Nothing to see here. Go to /video/room/:room/participant/:participant instead. Make sure to pass in the roomId (Use any unique ID. We will create a room for you or use it if it's already been created) and participantId which could be your FirstnameLastName</div>
}

export default Home;